# Newsletter Front

## Configuration

The front has `.env` file with available options:

- `VITE_SERVER_HOST` is the host of the newsletter's server
- `VITE_SERVER_PORT` is the port of the newsletter's server
